{
  "name": "practice-api-tests",
  "private": true,
  "scripts": {
    "test:api01": "newman run tests/api/messages.json -e tests/api/env.ci.json --reporters cli,junit,htmlextra --reporter-junit-export reports/api01-junit.xml --reporter-htmlextra-export reports/api01-report.html",
    "test:api02": "newman run tests/api/replies.json -e tests/api/env.ci.json --reporters cli,junit,htmlextra --reporter-junit-export reports/api02-junit.xml --reporter-htmlextra-export reports/api02-report.html",
    "test:api03": "newman run tests/api/change-password.json -e tests/api/env.ci.json --reporters cli,junit,htmlextra --reporter-junit-export reports/api03-junit.xml --reporter-htmlextra-export reports/api03-report.html",
    "test:all": "rm -rf reports && mkdir -p reports && npm run test:api01 && npm run test:api02 && npm run test:api03"
  },
  "devDependencies": {
    "newman": "^6.2.0",
    "newman-reporter-htmlextra": "^1.23.0"
  }
}
